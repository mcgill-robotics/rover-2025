# Teleop System Python Dependencies
# Combined requirements for all teleop services, tests, and utilities

# ===== ROS2 Dependencies =====
# NOTE: ROS2 packages (rclpy, std_msgs, geometry_msgs) are installed via system packages
# Make sure to source your ROS2 environment: source /opt/ros/humble/setup.bash
# These are not installed via pip but through the ROS2 system installation

# ===== Web Service Dependencies =====
fastapi==0.104.1
uvicorn[standard]==0.24.0
aiohttp>=3.8.0
aiohttp-cors>=0.7.0
websockets==12.0
python-multipart==0.0.6

# ===== Computer Vision & Camera Processing =====
opencv-contrib-python==4.8.1.78
# Note: If you encounter conflicts, uninstall other opencv packages first:
# pip uninstall opencv-python opencv-python-headless opencv-contrib-python -y
# pip install --no-cache-dir opencv-contrib-python

# ===== GStreamer & PyGObject =====
PyGObject>=3.42.0
# Note: On macOS with brew, you may need to install system dependencies:
# brew install pygobject3 gtk+3 gstreamer gst-plugins-base gst-plugins-good gst-plugins-bad gst-plugins-ugly

# ===== Data Processing & Scientific Computing =====
numpy>=1.23.0,<2.0

# ===== Async Programming =====
asyncio

# ===== Utilities & Logging =====
python-dateutil>=2.8.0

# ===== Testing Dependencies =====
unittest2>=1.1.0
pytest>=7.0.0
pytest-asyncio>=0.21.0

# ===== Development Dependencies =====
# Uncomment for development
# black>=22.0.0
# flake8>=4.0.0
# mypy>=0.950
